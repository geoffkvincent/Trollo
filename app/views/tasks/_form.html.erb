
<h1><%= @task.id ? 'Edit Task' : 'Create Task' %></h1>
<%= form_for [@list, @task] do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, required: true %>
  <br />
  <%= f.label :body %>
  <%= f.text_area :body %>
  <br />
  <%= f.submit %>
<% end %>